program square();
var first : integer;
    second : integer;
    third : integer;
    fourth : real;
function squared(a:integer) : integer
    begin
        return a * a * third * first
    end;
begin
    first := 2 + 2;
    second := 4 * fourth;
    print(squared(first));
    print(squared(squared(second)))
end.